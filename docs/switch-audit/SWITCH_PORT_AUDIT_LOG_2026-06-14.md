# Switch Port Audit Log - 2026-06-14

## Scope

This log summarizes the Codex conversation and build work for the GTA IV / LibertyRecomp static recompilation Nintendo Switch audit.

This is not a playable Switch port. The current milestone is configure/build/link progress under devkitPro/devkitA64.

## Starting Context

- Main repo: `C:\Users\Jellybone\Documents\GitHub\LibertyRecomp`
- Workspace: `C:\Users\Jellybone\Documents\Codex\2026-06-12\d-gta4-ns`
- Switch build dir: `C:\Users\Jellybone\Documents\Codex\2026-06-12\d-gta4-ns\work\liberty-switch-audit-debug`
- devkitPro: `C:\devkitPro`
- devkitA64: `C:\devkitPro\devkitA64`
- Reference repo: `givethesourceplox/UnleashedRecomp-NX`
- Local Nintendo SDK/reference notes:
  `C:\Users\Jellybone\Documents\Codex\2026-06-12\d-gta4-ns\NINTENDO_SDK_USAGE_NOTES_2026-06-14.md`

Before this session:

- Switch CMake configure already succeeded.
- `LibertyRecompLib` already built as a Switch static library.
- Switch main executable was still failing through UI/install/runtime/link blockers.
- Several systems were already stubbed for audit: graphics, audio, installer/desktop UI, file mapping, guest memory/page protection.

## Build Result

Switch main executable now compiles and links as a devkitA64 ELF:

`C:\Users\Jellybone\Documents\Codex\2026-06-12\d-gta4-ns\work\liberty-switch-audit-debug\LibertyRecomp\LibertyRecomp`

Latest confirmed size:

- `641,573,264` bytes
- Last write time: `2026-06-14 22:27:54`
- Build ID: `6993231b75a6dbe36dd72e48a819a4f4ee2c17bf`

Switch packaging now also emits an NRO audit artifact:

`C:\Users\Jellybone\Documents\Codex\2026-06-12\d-gta4-ns\work\liberty-switch-audit-debug\LibertyRecomp\LibertyRecomp.nro`

Latest confirmed size:

- `160,917,442` bytes
- Last write time: `2026-06-14 22:27:54`

Switch packaging now also emits a full ExeFS/NPDM audit package:

`C:\Users\Jellybone\Documents\Codex\2026-06-12\d-gta4-ns\work\liberty-switch-audit-debug\LibertyRecomp\LibertyRecompExefs.nsp`

Latest confirmed size:

- ExeFS PFS0 package: `64,971,186` bytes
- Last write time: `2026-06-14 22:27:56`
- Build ID: `6993231b75a6dbe36dd72e48a819a4f4ee2c17bf`

The generated NACP is:

`C:\Users\Jellybone\Documents\Codex\2026-06-12\d-gta4-ns\work\liberty-switch-audit-debug\LibertyRecomp\LibertyRecomp.nacp`

Latest confirmed size:

- `16,384` bytes
- Last write time: `2026-06-14 15:28:47`

An independent minimal boot probe NRO is also available:

`C:\Users\Jellybone\Documents\Codex\2026-06-12\d-gta4-ns\work\liberty-switch-audit-debug\LibertyRecomp\LibertyRecompBootProbe.nro`

Latest confirmed size:

- ELF: `2,983,232` bytes
- NRO: `300,434` bytes
- Last write time: `2026-06-14 20:53:50`
- Build ID: `d32811733bd2fe5c2e8172085c607fb1379c2903`
- `readelf -dW` reports no `TEXTREL`.

An independent guest-memory SVC probe NRO is also available:

`C:\Users\Jellybone\Documents\Codex\2026-06-12\d-gta4-ns\work\liberty-switch-audit-debug\LibertyRecomp\LibertyRecompMemoryProbe.nro`

Latest confirmed size:

- ELF: `2,990,088` bytes
- NRO: `304,530` bytes
- Last write time: `2026-06-14 22:03:29`
- Build ID: `8bccca91e6e3e02b0cf1b487eb2f42794fa224ef`

An independent guest-memory ExeFS/NPDM probe package is also available:

`C:\Users\Jellybone\Documents\Codex\2026-06-12\d-gta4-ns\work\liberty-switch-audit-debug\LibertyRecomp\LibertyRecompMemoryProbeExefs.nsp`

Latest confirmed size:

- ExeFS PFS0 package: `159,091` bytes
- NSO `main`: `157,845` bytes before the latest source rebuild, regenerated by Ninja as needed
- `main.npdm`: `944` bytes
- Last write time: `2026-06-14 22:13:52`
- Build ID: `a528c82fb5a99ebcb4b794ad16cde8defac33fc5`

Successful command:

```powershell
& "C:\Users\Jellybone\Documents\Codex\2026-06-12\d-gta4-ns\work\bin\ninja.exe" -C "C:\Users\Jellybone\Documents\Codex\2026-06-12\d-gta4-ns\work\liberty-switch-audit-debug" -j 2 LibertyRecompNro
```

Ninja exit code was `0`.

Build-directory note:

- Earlier interrupted/timeout-wrapped Ninja invocations left `.ninja_deps` incomplete.
- The NRO was still produced successfully, but a later dry-run continued to report missing deps and would conservatively rebuild many objects.
- This is build-directory state, not a new source compile failure.

## Important Caveat

The produced ELF and NRO are audit build artifacts, not a playable game.

Current work remains a compile/audit pass. Runtime systems still need real Switch design and implementation.

## Major Changes Made

- Added/used Switch OS source branch in `LibertyRecomp/CMakeLists.txt`.
- Included existing Switch OS source files:
  - `os/switch/logger_switch.cpp`
  - `os/switch/media_switch.cpp`
  - `os/switch/process_switch.cpp`
  - `os/switch/user_switch.cpp`
  - `os/switch/version_switch.cpp`
- Added Switch registry no-op include:
  - `os/switch/registry_switch.inl`
- Added Switch platform paths under `sdmc:/switch/LibertyRecomp`.
- Added minimal XenonUtils sources directly to the Switch main target for image/XDBF parsing.
- Disabled ImGui shell functions on Switch with `IMGUI_DISABLE_DEFAULT_SHELL_FUNCTIONS`.
- Fixed C linkage for `__imp__XamUserGetSigninState`.
- Added Switch audit ReXGlue runtime/CRT stubs.
- Added Switch audit GameNetworkingSockets stubs.
- Removed the temporary `-Wl,-z,notext` bypass.
- Added devkitA64 PIC library search path before the non-PIC devkitA64 library path so Switch links use PIC libstdc++/newlib runtime archives where available.
- Kept libnx `-z text`; final link succeeds without `TEXTREL`.
- Added a generated build-local `liberty-switch.specs` and build-local copy of `libnx/switch.ld`, avoiding `switch.specs` fallback to `/opt/devkitpro/libnx/switch.ld` when Ninja runs without `DEVKITPRO` in the environment.
- Added `-z gcs=never` to the generated Switch specs. This marks the output as not using Arm GCS for the Switch target, but it does not remove GCC 15 libgcc's internal `gcspr_el0` instruction from the unwinder.
- Added `LibertyRecompNro` CMake target:
  - generates `LibertyRecomp.nacp` with `nacptool`,
  - packages `LibertyRecomp.nro` with `elf2nro`,
  - embeds `LibertyRecompResources/images/game_icon.png` via `elf2nro --icon`,
  - embeds an empty build-directory RomFS placeholder.
- Added independent minimal boot probe packaging:
  - `os/switch/boot_probe_switch.c` is a tiny C-only libnx program that shows a visible console screen with `fsdevMountSdmc()`, `mkdir()`, `stat()`, and `fopen()` results, writes `sdmc:/switch/LibertyRecomp/boot_probe.log` when possible, and sleeps indefinitely.
  - `LibertyRecompBootProbeNro` packages `LibertyRecompBootProbe.nro` with title `Liberty Boot Probe`.
  - This target intentionally does not link LibertyRecomp, generated GTA IV code, SDL, C++ runtime-heavy paths, or the large global/static initialization path.
- Added independent guest-memory SVC probe packaging:
  - `os/switch/memory_probe_switch.c` logs memory-related `svcGetInfo()` values and tests `svcMapPhysicalMemory()` against small ASLR ranges, 4 GiB reservation windows, and fixed heap-region candidates.
  - `LibertyRecompMemoryProbeNro` packages `LibertyRecompMemoryProbe.nro` with title `Liberty Memory Probe`.
  - This target intentionally does not run game code; it exists only to isolate Switch guest-memory SVC behavior from the LibertyRecomp runtime.
- Added independent guest-memory ExeFS/NPDM probe packaging:
  - `os/switch/memory_probe_npdm.json` sets `system_resource_size=0x08000000` for the memory probe.
  - `LibertyRecompMemoryProbeExeFs` converts the probe ELF to NSO, generates `main.npdm`, and packages an ExeFS PFS0 named `LibertyRecompMemoryProbeExefs.nsp`.
  - This package is an audit probe, not a final installable game package.
- Added full LibertyRecomp ExeFS/NPDM audit packaging:
  - `os/switch/liberty_recomp_npdm.json` initially set `system_resource_size=0x08000000` for the full app; later 2026-06-15 guest-memory audit work raised it to `0x10000000`.
  - `LibertyRecompExeFs` converts the full `LibertyRecomp` ELF to NSO, generates `main.npdm`, and packages an ExeFS PFS0 named `LibertyRecompExefs.nsp`.
  - This package is an audit package, not a final hacpack-produced installable game NSP.
- Replaced the Switch OS version audit stub:
  - `os/switch/version_switch.cpp` now returns libnx `hosversionGet()` major/minor/micro values.
  - The direct `switch/runtime/hosversion.h` include is wrapped in `extern "C"` so C++ references link against libnx's C ABI symbol.
- Added basic Switch RomFS mount lifecycle:
  - `main.cpp` calls `romfsMountSelf("romfs")` during Switch startup and `romfsUnmount("romfs")` on normal return.
  - Current SD-only audit packages skip `romfsMountSelf()` when libnx `envIsNso()` reports true. This avoids Ryujinx's no-RomFS ExeFS PFS0 failure while the audit content path remains `sdmc:/switch/LibertyRecomp`.
  - The code uses direct C ABI declarations to avoid pulling libnx's global `Mutex` typedef into `main.cpp`.
- Added basic Switch SD mount and audit diagnostics:
  - `main.cpp` calls `fsdevMountSdmc()` early in Switch startup and unmounts `sdmc` on normal return.
  - Switch audit diagnostics are appended to `sdmc:/switch/LibertyRecomp/LibertyRecomp.log` when possible and also sent through `svcOutputDebugString()`.
  - Missing `default.xex` now stops before `KiSystemStartup()` / `GuestThread::Start()` instead of falling through as if installation had succeeded.
  - Current test build writes `Switch audit package startup; continuing to content preflight.`, checks `sdmc:/switch/LibertyRecomp/game/default.xex`, and stops on a visible diagnostic before host startup or guest code if content is missing.
  - If `default.xex` is present while Switch guest memory remains disabled, the build stops on a visible `Guest memory disabled` diagnostic before `KiSystemStartup()`.
- Added a Switch visible diagnostic screen for audit stops:
  - `os/switch/audit_diagnostics_switch.cpp` uses libnx `consoleInit()` / `consoleUpdate()`.
  - Missing-content and module-load failure paths display the reason, expected path, and log path before returning.
  - The helper no longer accepts controller input or auto-exits; use HOME/close software during audit testing. The current missing-content and guest-memory-disabled stops use this helper.
- Hardened the Switch installer stubs:
  - `install/installer_switch_stub.cpp` now checks for the module with C `fopen()` instead of always returning true.
  - `ui/installer_wizard_switch_stub.cpp` now returns false and reports that the Switch installer wizard is not implemented.
- Added a Switch-only sparse guest memory audit backend:
  - `kernel/memory.cpp` no longer tries to allocate the full 4 GiB PPC guest address space with C++ `new[]`.
  - It now queries the process Alias region with `svcGetInfo()` and maps selected ranges there with `svcMapPhysicalMemory()`.
  - It maps a 64 MiB low guest heap window, the 64 KiB XMA I/O window, a 32 MiB physical heap window from `0x80000000` to `PPC_IMAGE_BASE`, and the XEX image/function-table range.
  - It defines the full app `__nx_heap_size` as 16 MiB for this audit target. The independent memory probe keeps its own 96 MiB heap override.
  - `kernel/heap.cpp` caps Switch `o1heap` arenas to the mapped audit windows instead of assuming the full desktop 4 GiB backing store is committed.
- The independent memory probe now shows this sparse SVC path is blocked under the current NRO/hbloader environment because the process has no NPDM system-resource budget (`SystemResourceSizeTotal=0`), not because the process lacks ordinary memory.
- The ExeFS/NPDM memory probe now shows the sparse SVC path can map Alias region pages when `system_resource_size` is present. ASLR/heap-region targets return `0x0000DC01`, but Alias targets such as `0x80000000` and `0x102000000` return success and are writable.
- Audited `D:\Nintendo` as a local official Nintendo SDK/reference tree.
  - Created `NINTENDO_SDK_USAGE_NOTES_2026-06-14.md`.
  - Key reference areas: NX add-on samples for `Audio*`, `Fs*`, `Hid*`, `Socket*`, `Os*`, `Gfx*`, and `Nvn*`; official docs under `NintendoSDK_Documents-ForNX`; host tools such as `CrashReport.exe`, `ConvertNxbinlog.exe`, `MakeMeta.exe`, `HidShell.exe`, and `BackupSaveData.exe`.
  - Important constraint: keep official SDK headers/libraries/samples out of the current devkitPro/libnx NRO target. Use them for local behavior/reference only unless a separate licensed official-SDK target is created.

## Stub Tracking

Temporary stubs are tracked in:

`C:\Users\Jellybone\Documents\Codex\2026-06-12\d-gta4-ns\SWITCH_STUBS.md`

Any new stub must be added there. Any replaced stub must be removed or marked resolved there.

## New / Notable Audit Stubs

- `LibertyRecompLib/rexglue_runtime_stubs.cpp`
  - ReXGlue clock/logging/MMIO/thread TLS stubs.
  - Partial native guest-memory `rexcrt_*` string/memory helpers.
  - File, directory, heap, and file-time `rexcrt_*` failure/no-op stubs.
- `LibertyRecomp/kernel/io/p2p_manager_switch_stub.cpp`
  - `GameNetworkingSockets_Init()` returns false.
  - P2P networking remains disabled on Switch audit builds.
- `LibertyRecomp/os/switch/registry_switch.inl`
  - Registry read/write/init return false.
- `LibertyRecomp/os/switch/process_switch.cpp`
  - Process launch is no-op.
  - Executable path/root/working directory are simple RomFS/SD guesses.
- `LibertyRecomp/CMakeLists.txt`
  - Switch audit PIC library search path.
  - Switch audit `LibertyRecompNro` target.
- `LibertyRecomp/main.cpp`
  - Switch audit `romfs:` / `sdmc:` mount lifecycle and early missing-module guard.
  - Current test build writes `sdmc:/switch/LibertyRecomp/LibertyRecomp.log`, runs the early missing-module preflight, and stops on visible diagnostics before host/guest startup when content or guest memory is not ready.
- `LibertyRecomp/os/switch/audit_diagnostics_switch.cpp`
  - Visible libnx framebuffer console diagnostic screen for missing-content and module-load audit stops.
  - It no longer auto-exits or accepts controller input.
  - Skips the project PCH to avoid libnx `Mutex` typedef conflicts with the project's `Mutex` alias.
- `LibertyRecomp/os/switch/boot_probe_switch.c`
  - Minimal C-only boot probe packaged as `LibertyRecompBootProbe.nro`.
  - Shows a visible console screen with SD mount and file-operation diagnostics, writes `sdmc:/switch/LibertyRecomp/boot_probe.log` when possible, and sleeps indefinitely.
  - Used only to determine whether the real hardware/Sphaira launch path can run a minimal NRO and write to SD.
- `LibertyRecomp/os/switch/memory_probe_switch.c`
  - Minimal C-only memory probe packaged as `LibertyRecompMemoryProbe.nro`.
  - Logs `svcGetInfo()` memory/system-resource values and tests `svcMapPhysicalMemory()` without running LibertyRecomp game code.
  - Used only to determine whether the NRO/hbloader environment can support sparse guest-memory mapping.
- `LibertyRecomp/install/installer_switch_stub.cpp`
  - Only `checkGameInstall()` does a real module-exists check; the rest of the installer remains stubbed.
- `LibertyRecomp/ui/installer_wizard_switch_stub.cpp`
  - Switch installer wizard remains unimplemented and returns false.
- `LibertyRecomp/kernel/memory.cpp` and `LibertyRecomp/kernel/heap.cpp`
  - Switch sparse guest memory and capped o1heap windows are audit scaffolding only.
  - Unmapped PPC guest pages can still Data Abort until a real page-backed guest memory design exists.
- `toolchains/switch-libnx.cmake`
  - Generated absolute-path Switch specs file and build-local `switch.ld` copy for stable link script resolution.

## Linker Notes

`switch.specs` supplies `-z text`, which rejects dynamic relocations in read-only segments.

The audit build no longer overrides this with `-Wl,-z,notext`.

The stock libnx `switch.specs` also resolves `switch.ld` through the `DEVKITPRO` environment variable. This build now generates a local specs file and local copy of `switch.ld` during CMake configure so Ninja does not fail with `/opt/devkitpro/libnx/switch.ld` on Windows when run from a process without `DEVKITPRO`.

Initial relocation audit:

- `readelf -d` still reports `TEXTREL`.
- There is no `.rela.dyn`; dynamic relocation data is `.relr.dyn`.
- `.relr.dyn` has `1,950` encoded entries and relocates `63,326` locations.
- Target range count from `readelf -rW`:
  - `.text` executable LOAD: `0`
  - read-only LOAD before RELRO overlap: `1,662`
  - RELRO overlap range: `4,975`
  - writable LOAD: `56,689`
- First read-only examples are libstdc++ RTTI/vtable symbols such as `_ZTISt9bad_alloc` and `_ZTVSt9bad_alloc`, so the current `-z,notext` debt is not literal `.text` patching but read-only dynamic relocations in C++ metadata/static pointer tables.
- Raw relocation dump saved at:
  `C:\Users\Jellybone\Documents\Codex\2026-06-12\d-gta4-ns\work\liberty-switch-audit-debug\LibertyRecomp\LibertyRecomp.relr.txt`

Resolution:

- Switch link now searches `C:\devkitPro\devkitA64\aarch64-none-elf\lib\pic` before `C:\devkitPro\devkitA64\aarch64-none-elf\lib`.
- Removing `-Wl,-z,notext` initially reproduced the expected linker failure: `read-only segment has dynamic relocations`.
- After preferring PIC devkitA64 libraries, the final build links successfully with libnx `-z text`.
- Final `readelf -dW` no longer reports `TEXTREL`.
- Final program headers keep the libnx layout:
  - `LOAD0`: `R E`, `.text/.init/.plt/.fini`
  - `LOAD1`: `R`, `.nx-module-name/.relr.dyn/.hash/.dynsym/.dynstr/.rodata/...`
  - `LOAD2`: `RW`, `.init_array/.fini_array/.data.rel.ro/.dynamic/.got/.data/.bss/...`
- Final RELR target classification:
  - `LOAD0 R E`: `0`
  - `LOAD1 R`: `0`
  - `LOAD2 RW`: `63,667`
  - `outside`: `0`

Crash log audit:

- Hardware dump `C:\Users\Jellybone\Desktop\log\01781420466_010000000000100d.log` was an Atmosphere `Data Abort` in `hbloader`, Program ID `010000000000100d`.
- Crash-time PC was `LibertyRecomp + 0x8c2ac20`, LR was `LibertyRecomp + 0x8c2aab8`, base register X4 was `LibertyRecomp + 0x0`, and X2 was `0x8e97010`.
- The crash-time PC/LR matched libnx dynamic relocation processing (`__nx_dynamic` / `_dynProcessRelr`) in the old build, and `base + 0x8e97010` landed in the old read-only metadata range. This matches the old `TEXTREL`/read-only RELR debt.
- Ryujinx Canary 1.3.269 was also tested with the new NRO. It loaded Build ID `4F29CE8A5BED8BDED8F202DEA479C14C72F7E996...` and got past homebrew loading, then aborted with `Unknown MRS 0xD53B2521 at 0x00000000111B19B8`.
- With Ryujinx loading the NRO at `0x8500000`, the new Ryujinx crash maps to ELF offset `0x8cb19b8`, `_Unwind_RaiseException_Phase2`, instruction `mrs x1, gcspr_el0` from GCC 15 libgcc. `-z gcs=never` did not remove that prebuilt libgcc unwinder instruction.
- Treat the current Ryujinx failure as a simulator/toolchain blocker separate from the old hardware RELR write-to-read-only crash. A real hardware retest is needed for the new NRO.
- Hardware retest dump `C:\Users\Jellybone\Desktop\log\2\01781423595_010000000000100d.log` used Build ID `4F29CE8A5BED8BDED8F202DEA479C14C72F7E996...` and no longer matched the old RELR write-to-read-only crash.
- The new hardware PC `LibertyRecomp + 0x8cb25a8` and LR `LibertyRecomp + 0x8cb2570` mapped to GCC libgcc `fde_radixsort()` during C++ exception unwinding. The stack traced through `_Unwind_Find_FDE`, `_Unwind_RaiseException`, `__cxa_throw`, `operator new[]`, and `Memory::Memory()` at `kernel/memory.cpp:30`, called by global `Memory g_memory` initialization in `main.cpp`.
- Root cause for this phase: the Switch branch attempted `new (std::nothrow) uint8_t[0x100000000]` during global construction. The allocation failed and still entered the C++ exception/unwinder path inside libstdc++/libgcc, causing a hardware Data Abort before `main()`.
- The ERPT-style report `C:\Users\Jellybone\Desktop\log\6dcc4701-7236-1043-73ab-631984d0dad1` appears to be a system/frontend report mentioning `sphaira`; it did not include a useful LibertyRecomp PC/LR stack. The Atmosphere fatal log above was the actionable dump.
- Follow-up build `52c6b749b25c1d4ee06ba6bdb4431205ceed22ed` replaces the 4 GiB C++ allocation with sparse Switch guest-memory reservation/mapping and rebuilds `LibertyRecompNro` successfully.
- Ryujinx Canary 1.3.269 test of Build ID `52c6b749b25c1d4ee06ba6bdb4431205ceed22ed` loaded the NRO as homebrew, loaded image 0 at `0x8500000`, created a VI layer, and then logged `ServiceAm Exit` around 7 seconds. It did not reproduce the earlier `Unknown MRS gcspr_el0` unwinder abort. This is an emulator smoke-test only; it likely exited through an installer/graphics/stub path and does not imply gameplay.
- Follow-up build `5c7d712fb408d740c9c22f0ca6696fe65a96287b` adds SD mounting, missing-module guarding, and honest Switch installer failure behavior.
- Ryujinx Canary 1.3.269 test of Build ID `5c7d712fb408d740c9c22f0ca6696fe65a96287b` loaded the NRO as homebrew, loaded image 0 at `0x8500000`, found the matching build ID, created a VI layer, and logged `ServiceAm Exit` around 6 seconds. No new Ryujinx abort/exception was logged.
- The attempted audit log `sdmc:/switch/LibertyRecomp/LibertyRecomp.log` was not found under `D:\Games\Ryujinx\Ryujinx\portable\sdcard` after the emulator run. Treat SD audit logging as a hardware verification item; the code path is still useful on real homebrew where `fsdevMountSdmc()` succeeds.
- Hardware retest of Build ID `5c7d712fb408d740c9c22f0ca6696fe65a96287b` did not system-crash. The app cleanly returned to the Homebrew/Menu page and produced software report `C:\Users\Jellybone\Desktop\ed9d2c85-a785-9c48-5f81-92a5be07987a`.
- Parsed report `ed9d2c85-a785-9c48-5f81-92a5be07987a` is a 200-entry MessagePack-style system/application report, not an Atmosphere fatal dump. Key fields: `ApplicationTitle=Liberty Recomp`, `ApplicationID=05f3e1d32e2a8000`, `ApplicationStorageLocation=SdCard`, `ErrorCode=2128-0051`, `AbortFlag=true`, `ApplicationAbortFlag=true`, `CreateProcessFailureFlag=false`, `SdCardMountStatus=0x00000000`, `OccurrenceTimestamp=2026-06-14 16:46:21` local time.
- The hardware report contains no PC/LR/register context. Treat this as confirmation that the previous fatal startup crash path is gone for the guarded NRO; the remaining behavior is expected early application abort/exit from the audit container path.
- Follow-up build `496fa2f332624750b943c4d60d978ac343b138c9` adds the visible libnx framebuffer diagnostic screen for missing-content and module-load audit stops.
- Ryujinx Canary 1.3.269 test of Build ID `496fa2f332624750b943c4d60d978ac343b138c9` loaded the NRO, created a VI layer, and stayed alive until the 12-second test timeout, where it was manually killed. The log `D:\Games\Ryujinx\Ryujinx\portable\Logs\Ryujinx_Canary_1.3.269_2026-06-14_17-02-00.log` did not contain `ServiceAm Exit`, `Unknown MRS`, fatal exception, or data abort before the forced stop.
- Ryujinx still did not show `sdmc:/switch/LibertyRecomp/LibertyRecomp.log` under the tested portable SD directory. Hardware should verify both the visible diagnostic screen and whether the SD log appears on the actual SD card.
- Hardware retest of Build ID `496fa2f332624750b943c4d60d978ac343b138c9` still black-screened then returned without creating `sdmc:/switch/LibertyRecomp/LibertyRecomp.log`. The user-provided report `C:\Users\Jellybone\AppData\Local\Temp\03034866-2b22-2649-6ffe-b06c5a6f96e0` again parsed as a software/application report: `ApplicationTitle=Liberty Recomp`, `ApplicationID=05f3e1d32e2a8000`, `ErrorCode=2128-0051`, `AbortFlag=true`, `ApplicationAbortFlag=true`, `CreateProcessFailureFlag=false`, `ApplicationAliveTime=0`, and no PC/LR/register fatal context.
- The initial 12-second Ryujinx smoke test was not sufficient for the visible diagnostic path because the intended diagnostic hold was longer than the effective guest runtime window. A longer run showed `ServiceAm Exit` around guest time 6 seconds.
- Follow-up Build ID `49a186c46a997ff3ba44bc95d296e3144d94c31b` changed the visible diagnostic helper into a non-auto-exiting, non-input-exiting hard stop. Ryujinx still logged `ServiceAm Exit` around guest time 6 seconds.
- Follow-up Build ID `d2762bb16fd23d3d11066b567ca9759081ac5f7d` added a `main()` entry hard stop immediately after successful SD mount and before host startup/content checks. Ryujinx still logged `ServiceAm Exit` around guest time 6 seconds.
- Follow-up Build ID `b752451d918154096010f06a54f3d4354d99eaa8` changed `main()` to a pure first-line `svcSleepThread()` infinite loop with no SD mount, no console, and no installer/configuration work. Ryujinx still logged `ServiceAm Exit` around guest time 6 seconds, so treat Ryujinx Canary 1.3.269 as unreliable for validating this specific homebrew hold behavior.
- Earlier test build `afa9072da6ad53ee46d05bfc7a9aadf901e8eff4` wrote `Switch audit NRO startup; SD-log sleep hard stop.` to `sdmc:/switch/LibertyRecomp/LibertyRecomp.log`, unmounted SD/RomFS, and slept indefinitely before configuration, installer, content checks, or guest startup. This artifact has been superseded by the content-preflight build.
- Hardware retest of Build ID `afa9072da6ad53ee46d05bfc7a9aadf901e8eff4` still produced no SD log and behaved like the prior software-abort cases. The user-provided report `C:\Users\Jellybone\AppData\Local\Temp\02af45f2-d1c3-8b42-4db2-a180f0d13283` parsed as a 200-entry MessagePack-style application report. Key fields: `ReportIdentifier=f245af02-c3d1-428b-4db2-8332d1f080a1`, `OccurrenceTimestamp=2026-06-14 20:33:44` local time, `ApplicationTitle=Liberty Recomp`, `ApplicationID=05f3e1d32e2a8000`, `ApplicationStorageLocation=SdCard`, `ErrorCode=2128-0051`, `AbortFlag=true`, `ApplicationAbortFlag=true`, `CreateProcessFailureFlag=false`, `ApplicationAliveTime=0`, `SdCardMountStatus=0x00000000`, and `AcpApplicationErrorCodeCategory=sphaira`. No PC/LR/register fatal context or Build ID was present.
- Since `afa9072da6ad53ee46d05bfc7a9aadf901e8eff4` writes its SD log only after LibertyRecomp's large C++/libnx startup reaches `main()` and `fsdevMountSdmc()` succeeds, the absence of the log does not distinguish a main-before-failure from a Sphaira/front-end launch-path issue.
- Follow-up boot probe Build ID `5208ed125bee8ea041514c749fef43d4573bd86a` was added to split that question. It is a tiny C-only NRO that writes `sdmc:/switch/LibertyRecomp/boot_probe.log` and sleeps indefinitely without linking or initializing LibertyRecomp.
- Ryujinx Canary 1.3.269 smoke test of `LibertyRecompBootProbe.nro` loaded Build ID `5208ed125bee8ea041514c749fef43d4573bd86a`, did not log fatal/exception/data abort before the forced 8-second stop, and printed `OutputDebugString: [SwitchBootProbe] entered main; fsdevMountSdmc=0x00000559`. No `boot_probe.log` appeared under the tested Ryujinx portable SD tree because the probe's SD mount failed in Ryujinx. Real hardware should still validate hold behavior and SD mount/write behavior.
- Hardware retest of the first boot probe Build ID `5208ed125bee8ea041514c749fef43d4573bd86a` held on hardware but did not create `sdmc:/switch/LibertyRecomp/boot_probe.log`. This proves the launch path can enter and hold in a minimal NRO on real hardware, but SD/file-write status still needs visible diagnostics.
- Follow-up visible boot probe Build ID `d4f9713bc86bb148ef9694404d9219b57c0d63d2` now displays `fsdevMountSdmc`, `mkdir sdmc:/switch`, `mkdir LibertyRecomp`, `stat LibertyRecomp`, and `fopen boot_probe.log` results directly on screen, then sleeps indefinitely.
- Ryujinx Canary 1.3.269 smoke test of visible boot probe Build ID `d4f9713bc86bb148ef9694404d9219b57c0d63d2` loaded the NRO, did not log fatal/exception/data abort before the forced 8-second stop, and again printed `OutputDebugString: [SwitchBootProbe] entered main; fsdevMountSdmc=0x00000559`.
- Hardware visible boot probe test showed the same important lines: `fsdevMountSdmc: 0x00000559`, `SD mount failed; file write skipped`, and `No game code is running`. This proved the probe held on hardware, but the probe had incorrectly skipped file operations when `fsdevMountSdmc()` returned nonzero.
- The boot probe was updated to check whether `sdmc:` is already registered before and after calling `fsdevMountSdmc()`. New visible boot probe Build ID: `d32811733bd2fe5c2e8172085c607fb1379c2903`.
- Ryujinx smoke test of boot probe Build ID `d32811733bd2fe5c2e8172085c607fb1379c2903` printed `sdmc_before=1 fsdevMountSdmc=0x00000559 sdmc_after=1` and successfully wrote `D:\Games\Ryujinx\Ryujinx\portable\sdcard\switch\LibertyRecomp\boot_probe.log`. Conclusion: `0x559` in this environment is compatible with `sdmc:` already being registered; it is not proof that SD writes are impossible.
- `main.cpp` was updated with the same existing-`sdmc:` detection. It now treats SD as usable if `fsdevGetDeviceFileSystem("sdmc")` or `stat("sdmc:/")` succeeds, calls `fsdevMountSdmc()` only as needed, and unmounts only devices it mounted itself.
- Full NRO Build ID `33717a6c3f6fb0bee0c068ddeec27e1a9b6a62d6` still did not write `LibertyRecomp.log` in Ryujinx. With additional breadcrumbs, Build ID `10993b131f39cd8e84d1b8562d25382e9f435e35` reached a pre-main constructor and `Memory::Memory()`, then failed the first sparse guest-memory low-heap mapping with `svcMapPhysicalMemory()` result `0x0000FA01` (`KernelError_InvalidState`).
- For startup-container audit builds, `kernel/memory.cpp` now leaves `Memory::base=nullptr` and skips guest-memory mapping unless `LIBERTY_RECOMP_SWITCH_ENABLE_GUEST_MEMORY_AUDIT` is defined. This is not a playable memory model; it only lets the NRO reach `main()` while the current build sleeps before guest/content startup.
- After disabling default guest-memory mapping, a temporary `__libc_init_array` wrapper identified the next pre-main exit as `_GLOBAL__sub_I__Z16g_executableRootB5cxx11` in `LibertyRecomp/user/paths.cpp`, caused by Switch global path initialization calling filesystem checks before `romfs:` is mounted.
- `LibertyRecomp/user/paths.cpp` now uses a Switch-specific lazy `GetUserPath()` path (`sdmc:/switch/LibertyRecomp`) and avoids pre-main `std::filesystem::exists()` checks against `romfs:`.
- The temporary `__libc_init_array` wrapper and extra global-constructor probes were removed after the pre-main path issue was fixed.
- Ryujinx smoke test of full NRO Build ID `e186b764db51b97f42d207bac4e711ab98233549` reached `main()`, printed:
  - `[Switch] premain constructor 101 entered.`
  - `[Switch][Memory] guest memory disabled for startup audit; base=null`
  - `[Switch] main entered.`
  - `[Switch] romfsMountSelf returned 0x00000000.`
  - `[Switch] ensuring sdmc.`
  - `[Switch] Switch audit NRO startup; SD-log sleep hard stop.`
- The same Ryujinx run wrote `D:\Games\Ryujinx\Ryujinx\portable\sdcard\switch\LibertyRecomp\LibertyRecomp.log` with `Switch audit NRO startup; SD-log sleep hard stop.` The NRO was manually killed after the smoke-test timeout. This confirms a Switch startup container reaches `main()` and writes SD logs in the emulator. It is still not playable.
- Follow-up full NRO Build ID `8a8cd9857cea1fffdc7ee4664a73691a744b8a2e` removed the SD-log sleep hard-stop, restored the early missing-content/default.xex diagnostic path, and added a guard that stops before host startup if `default.xex` exists but Switch guest memory is still disabled.
- Ryujinx smoke test of Build ID `8a8cd9857cea1fffdc7ee4664a73691a744b8a2e` used log:
  `D:\Games\Ryujinx\Ryujinx\portable\Logs\Ryujinx_Canary_1.3.269_2026-06-14_21-47-05.log`
  It reached `main()`, printed:
  - `[Switch] premain constructor 101 entered.`
  - `[Switch][Memory] guest memory disabled for startup audit; base=null`
  - `[Switch] main entered.`
  - `[Switch] romfsMountSelf returned 0x00000000.`
  - `[Switch] ensuring sdmc.`
  - `[Switch] Switch audit NRO startup; continuing to content preflight.`
  - `[Switch] Early preflight missing game executable: sdmc:/switch/LibertyRecomp/default.xex`
  - `[Switch] Expected SD layout root: sdmc:/switch/LibertyRecomp`
- The same Ryujinx run appended these lines to:
  `D:\Games\Ryujinx\Ryujinx\portable\sdcard\switch\LibertyRecomp\LibertyRecomp.log`
  No Ryujinx fatal/exception/data-abort line appeared before the manual smoke-test stop. This confirms the emulator reaches the missing-content diagnostic baseline. It is still not playable.
- Follow-up full NRO Build ID `bf943e8125cc1124b5bdd52cd152efed05c83cd6` aligned the Switch content preflight and `installer_switch_stub.cpp` with the existing desktop/runtime layout: `sdmc:/switch/LibertyRecomp/game/default.xex`.
- Added SD layout draft:
  `C:\Users\Jellybone\Documents\Codex\2026-06-12\d-gta4-ns\SWITCH_SD_LAYOUT.md`
- Ryujinx smoke test of Build ID `bf943e8125cc1124b5bdd52cd152efed05c83cd6` used log:
  `D:\Games\Ryujinx\Ryujinx\portable\Logs\Ryujinx_Canary_1.3.269_2026-06-14_21-52-30.log`
  It reached `main()`, printed:
  - `[Switch] premain constructor 101 entered.`
  - `[Switch][Memory] guest memory disabled for startup audit; base=null`
  - `[Switch] main entered.`
  - `[Switch] romfsMountSelf returned 0x00000000.`
  - `[Switch] ensuring sdmc.`
  - `[Switch] Switch audit NRO startup; continuing to content preflight.`
  - `[Switch] Early preflight missing game executable: sdmc:/switch/LibertyRecomp/game/default.xex`
  - `[Switch] Expected SD layout root: sdmc:/switch/LibertyRecomp`
  - `[Switch] Expected game content root: sdmc:/switch/LibertyRecomp/game`
- The same Ryujinx run appended those content-preflight lines to:
  `D:\Games\Ryujinx\Ryujinx\portable\sdcard\switch\LibertyRecomp\LibertyRecomp.log`
  No Ryujinx fatal/exception/data-abort line appeared before the manual smoke-test stop. This confirms the emulator reaches the corrected missing-content diagnostic baseline. It is still not playable.
- A second Ryujinx smoke test created a temporary dummy:
  `D:\Games\Ryujinx\Ryujinx\portable\sdcard\switch\LibertyRecomp\game\default.xex`
  The dummy was 5 bytes and was removed after the test. Ryujinx log:
  `D:\Games\Ryujinx\Ryujinx\portable\Logs\Ryujinx_Canary_1.3.269_2026-06-14_21-55-01.log`
  The NRO reached `main()` and appended:
  - `[Switch] Switch audit NRO startup; continuing to content preflight.`
  - `[Switch] default.xex was found, but Switch guest memory is disabled; stopping before host startup: sdmc:/switch/LibertyRecomp/game/default.xex`
  This confirms the current build does not enter host startup or `KiSystemStartup()` even if the module path exists. It is still not playable.
- Final full NRO artifact:
  - Path: `C:\Users\Jellybone\Documents\Codex\2026-06-12\d-gta4-ns\work\liberty-switch-audit-debug\LibertyRecomp\LibertyRecomp.nro`
  - Build ID: `bf943e8125cc1124b5bdd52cd152efed05c83cd6`
  - Size: `160,917,442` bytes
  - Last write time: `2026-06-14 21:51:58`
  - `readelf -dW` reports no `TEXTREL`.
- Added and built independent guest-memory SVC probe:
  - Source: `C:\Users\Jellybone\Documents\GitHub\LibertyRecomp\LibertyRecomp\os\switch\memory_probe_switch.c`
  - Target: `LibertyRecompMemoryProbeNro`
  - Artifact: `C:\Users\Jellybone\Documents\Codex\2026-06-12\d-gta4-ns\work\liberty-switch-audit-debug\LibertyRecomp\LibertyRecompMemoryProbe.nro`
  - Build ID: `8bccca91e6e3e02b0cf1b487eb2f42794fa224ef`
  - NRO size: `304,530` bytes
- Ryujinx smoke test of `LibertyRecompMemoryProbe.nro` used SD log:
  `D:\Games\Ryujinx\Ryujinx\portable\sdcard\switch\LibertyRecomp\memory_probe.log`
  and Ryujinx log:
  `D:\Games\Ryujinx\Ryujinx\portable\Logs\Ryujinx_Canary_1.3.269_2026-06-14_22-03-41.log`
- Memory probe result:
  - `sdmc_before=1 fsdevMountSdmc=0x00000559 sdmc_after=1 log=ok`
  - `Info TotalMemorySize=0x00000000cd500000`
  - `Info UsedMemorySize=0x0000000006143000`
  - `Info SystemResourceSizeTotal=0x0000000000000000`
  - `Info SystemResourceSizeUsed=0x0000000000000000`
  - `svcMapPhysicalMemory` returned `0x0000FA01` for small ASLR ranges, 4 GiB reservation windows, and fixed heap-region candidates.
- Root cause for the current sparse guest-memory blocker:
  - Reducing libnx/newlib heap to 96 MiB leaves ordinary memory available, so the immediate failure is not a normal RAM exhaustion problem.
  - The current NRO/hbloader launch environment provides zero system-resource budget, so `svcMapPhysicalMemory()` cannot back sparse guest pages.
  - The current generated PPC runtime assumes a contiguous raw `uint8_t* base` in generated function signatures and memory macros. A segmented backing store behind `Translate()` alone is not a drop-in replacement.
  - Next practical guest-memory branches are: investigate a non-NRO/title package path with NPDM `SystemResourceSize`, or plan a broader generated-code/runtime ABI redesign before guest startup.
- Added a `LibertyRecompMemoryProbeExeFs` target to test the non-NRO/NPDM branch without running LibertyRecomp game code:
  - NPDM JSON: `C:\Users\Jellybone\Documents\GitHub\LibertyRecomp\LibertyRecomp\os\switch\memory_probe_npdm.json`
  - Artifact: `C:\Users\Jellybone\Documents\Codex\2026-06-12\d-gta4-ns\work\liberty-switch-audit-debug\LibertyRecomp\LibertyRecompMemoryProbeExefs.nsp`
  - This is a devkitPro ExeFS PFS0 package produced by `elf2nso`, `npdmtool`, and `build_pfs0`; it is not the final hacpack-produced installable game NSP.
- Ryujinx smoke test of `LibertyRecompMemoryProbeExefs.nsp` used log:
  `D:\Games\Ryujinx\Ryujinx\portable\Logs\Ryujinx_Canary_1.3.269_2026-06-14_22-14-01.log`
  and appended to:
  `D:\Games\Ryujinx\Ryujinx\portable\sdcard\switch\LibertyRecomp\memory_probe.log`
- ExeFS/NPDM memory probe result:
  - Build ID: `a528c82fb5a99ebcb4b794ad16cde8defac33fc5`
  - Program ID: `01005F3E1D32E000`
  - `Info AliasRegionAddress=0x0000000080000000`
  - `Info AliasRegionSize=0x0000000180000000`
  - `Info SystemResourceSizeTotal=0x0000000008000000`
  - `Info SystemResourceSizeUsed=0x0000000000001000`
  - ASLR and heap-region `svcMapPhysicalMemory()` attempts returned `0x0000DC01` (`InvalidMemoryRegion`).
  - Alias-region `svcMapPhysicalMemory()` attempts at `0x80000000`, `0x80200000`, and `0x102000000` returned success, could be written/read, and could be unmapped.
- Updated `kernel/memory.cpp` sparse guest-memory audit backend to use the process Alias region as the 4 GiB guest base when `LIBERTY_RECOMP_SWITCH_ENABLE_GUEST_MEMORY_AUDIT` is enabled. Default startup-container builds still do not enable guest memory.
- Rebuilt the default full NRO after the Alias-region backend change:
  - Path: `C:\Users\Jellybone\Documents\Codex\2026-06-12\d-gta4-ns\work\liberty-switch-audit-debug\LibertyRecomp\LibertyRecomp.nro`
  - Build ID: `4647aaafb36da735accae452b2a767270a133075`
  - ELF size: `641,573,120` bytes
  - NRO size: `160,917,442` bytes
  - Last write time: `2026-06-14 22:16:34`
  - `readelf -dW` reports no `TEXTREL`.
- Ryujinx smoke test of full NRO Build ID `4647aaafb36da735accae452b2a767270a133075` used log:
  `D:\Games\Ryujinx\Ryujinx\portable\Logs\Ryujinx_Canary_1.3.269_2026-06-14_22-16-58.log`
  It reached the same default audit container path:
  - `[Switch][Memory] guest memory disabled for startup audit; base=null`
  - `[Switch] main entered.`
  - `[Switch] Switch audit NRO startup; continuing to content preflight.`
  - `[Switch] Early preflight missing game executable: sdmc:/switch/LibertyRecomp/game/default.xex`
  This confirms the default NRO still does not enter host startup or `KiSystemStartup()`.
- Added full `LibertyRecompExeFs` packaging with NPDM initially using `system_resource_size=0x08000000`; this was later raised to `0x10000000` during the 2026-06-15 function-table audit.
- Initial full ExeFS test of Build ID `45201fa6a02ae9102343de7231020577cff9530c` reached `main()` but crashed Ryujinx inside `romfsMountSelf()` because the devkitPro ExeFS PFS0 audit package has no RomFS data storage. The log was:
  `D:\Games\Ryujinx\Ryujinx\portable\Logs\Ryujinx_Canary_1.3.269_2026-06-14_22-23-33.log`
- `main.cpp` now skips `romfsMountSelf()` when `envIsNso()` reports true, and the startup log text now says `Switch audit package startup` instead of `Switch audit NRO startup`.
- Reduced the full app libnx/newlib heap override from 96 MiB to 16 MiB. The independent memory probe still uses its own 96 MiB heap override.
- Final full NRO/ExeFS artifact after those changes:
  - Build ID: `6993231b75a6dbe36dd72e48a819a4f4ee2c17bf`
  - ELF: `C:\Users\Jellybone\Documents\Codex\2026-06-12\d-gta4-ns\work\liberty-switch-audit-debug\LibertyRecomp\LibertyRecomp`
  - NRO: `C:\Users\Jellybone\Documents\Codex\2026-06-12\d-gta4-ns\work\liberty-switch-audit-debug\LibertyRecomp\LibertyRecomp.nro`
  - ExeFS PFS0: `C:\Users\Jellybone\Documents\Codex\2026-06-12\d-gta4-ns\work\liberty-switch-audit-debug\LibertyRecomp\LibertyRecompExefs.nsp`
  - ELF size: `641,573,264` bytes
  - NRO size: `160,917,442` bytes
  - ExeFS PFS0 size: `64,971,186` bytes
  - `readelf -dW` reports no `TEXTREL`.
- Ryujinx smoke test of final `LibertyRecompExefs.nsp` Build ID `6993231b75a6dbe36dd72e48a819a4f4ee2c17bf` used log:
  `D:\Games\Ryujinx\Ryujinx\portable\Logs\Ryujinx_Canary_1.3.269_2026-06-14_22-28-21.log`
  It reached the same default audit container path and appended SD log lines:
  - `[Switch][Memory] guest memory disabled for startup audit; base=null`
  - `[Switch] main entered.`
  - `[Switch] envIsNso reported true; skipping romfsMountSelf for SD-only audit package.`
  - `[Switch] Switch audit package startup; continuing to content preflight.`
  - `[Switch] Early preflight missing game executable: sdmc:/switch/LibertyRecomp/game/default.xex`
  This confirms the full ExeFS/NPDM audit package can start and reach content preflight. It is still not playable and still does not enter host startup or `KiSystemStartup()`.

## Remaining High-Risk Areas

- Graphics backend: Plume/Vulkan is still stubbed for Switch. Need deko3d or another Switch-specific backend.
- Audio: XMA/FFmpeg decode is stubbed. Need a real XMA decode/conversion strategy.
- Guest memory: current sparse Switch backing store/page protection behavior is audit-only. It is blocked in current NRO form because `SystemResourceSizeTotal=0` makes `svcMapPhysicalMemory()` fail with `0x0000FA01`, but the ExeFS/NPDM probe confirms `system_resource_size` plus Alias-region addresses can map pages successfully. Unmapped guest pages can still crash until a real page-backed design exists.
- Filesystem/VFS: real Switch SD/RomFS layout and install/content discovery still need design.
- NRO packaging: `elf2nro`/NACP flow exists, but icon, final NACP metadata, RomFS contents, SD layout, and runtime mount/init behavior are not final.
- Networking: GameNetworkingSockets/P2P is disabled.
- Runtime CRT/file/heap APIs: many ReXGlue `rexcrt_*` functions are no-op/failure stubs.

## Next Steps

1. Optional hardware retest of the latest full LibertyRecomp startup/content-preflight container:
   - Use `C:\Users\Jellybone\Documents\Codex\2026-06-12\d-gta4-ns\work\liberty-switch-audit-debug\LibertyRecomp\LibertyRecomp.nro`.
   - Expected Build ID: `6993231b75a6dbe36dd72e48a819a4f4ee2c17bf`.
   - Expected behavior without game content: visible `Missing game content` diagnostic, no gameplay UI, no automatic return, and `sdmc:/switch/LibertyRecomp/LibertyRecomp.log` contains the content-preflight and missing `sdmc:/switch/LibertyRecomp/game/default.xex` lines.
   - Exit it manually with HOME/close software after checking whether it holds and whether the log was written.
2. Continue from the content-preflight milestone:
   - Continue refining `C:\Users\Jellybone\Documents\Codex\2026-06-12\d-gta4-ns\SWITCH_SD_LAYOUT.md`. The current audit check only requires `sdmc:/switch/LibertyRecomp/game/default.xex`; this is not enough for gameplay.
   - If `game/default.xex` is present in the current build, expected behavior is a visible `Guest memory disabled` diagnostic before host startup.
   - Do not enter `KiSystemStartup()` / `GuestThread::Start()` until guest memory is reworked; `Memory::base` is intentionally null in the current startup-container build.
3. Resolve the guest-memory container/runtime decision before trying to run guest code:
   - Current NRO/hbloader memory probe reports `SystemResourceSizeTotal=0` and `svcMapPhysicalMemory()` returns `0x0000FA01` for every tested mapping pattern.
   - Current ExeFS/NPDM memory probe reports `SystemResourceSizeTotal=0x08000000` and Alias-region `svcMapPhysicalMemory()` succeeds.
   - Full LibertyRecomp now has a default ExeFS/NPDM audit target with `system_resource_size`, and it reaches content preflight with guest memory disabled.
   - Next build-system step: create a separate full LibertyRecomp guest-memory audit target or build preset that enables `LIBERTY_RECOMP_SWITCH_ENABLE_GUEST_MEMORY_AUDIT` only for ExeFS/NPDM, not for the default NRO.
   - Next runtime step: verify that full `Memory::Memory()` maps the planned low/XMA/physical/image windows in the Alias region, logs success/failure visibly, then stops before host startup.
   - Keep the generated-code/runtime memory ABI risk in view: the current raw contiguous `uint8_t* base` model works only if the 4 GiB guest window is represented as one Alias-region base. A segmented/accessor model remains a fallback, not the current primary branch.
4. Continue Ryujinx/runtime smoke testing:
   - Current Ryujinx Canary 1.3.269 no longer reproduces the earlier GCC 15 libgcc `gcspr_el0` unwinder abort for these diagnostic builds.
   - Do not use Ryujinx `ServiceAm Exit` around guest time 6 seconds alone as proof of app behavior; rely on `OutputDebugString` breadcrumbs and SD-log lines for this stage.
   - Continue using Ryujinx for load/build-ID/fatal checks, but rely on real hardware for SD-log and hold-behavior validation.
5. Improve the current Switch packaging path:
   - Refine final NACP metadata.
   - Define real RomFS layout and SD install/content layout.
   - Harden runtime RomFS mount/error behavior.
   - Use hacpack only after ExeFS/NPDM behavior is proven for full LibertyRecomp; do not copy keysets into the repo or hardcode them into build scripts.
6. Start replacing stubs from `SWITCH_STUBS.md`, prioritizing:
   - guest memory/page protection,
   - graphics backend,
   - filesystem/VFS,
   - audio/XMA,
   - runtime CRT/file/heap.
7. Use `NINTENDO_SDK_USAGE_NOTES_2026-06-14.md` as the map for safely using `D:\Nintendo` references when designing replacement systems.
8. Keep `AGENTS.md` and `SWITCH_STUBS.md` current after each session.

## Late Update: Full-Process Guest-Memory Audit

Added CMake switches for a separate full-process Switch guest-memory audit build:

- `LIBERTY_RECOMP_SWITCH_ENABLE_GUEST_MEMORY_AUDIT`
- `LIBERTY_RECOMP_SWITCH_GUEST_MEMORY_AUDIT_STOP`
- `LIBERTY_RECOMP_SWITCH_GUEST_LOW_MEMORY_AUDIT_SIZE`
- `LIBERTY_RECOMP_SWITCH_GUEST_PHYSICAL_HEAP_AUDIT_SIZE`
- `LIBERTY_RECOMP_SWITCH_GUEST_IMAGE_TABLE_AUDIT_SIZE`

The default startup-container build still leaves guest memory disabled. The guest-memory audit build uses a separate build directory:

`C:\Users\Jellybone\Documents\Codex\2026-06-12\d-gta4-ns\work\liberty-switch-guest-memory-audit-debug`

Build-system note:

- `toolchains/switch-libnx.cmake` can inject `liberty-switch.specs` through `CMAKE_EXE_LINKER_FLAGS` in fresh build directories.
- Older build directories may not have that cache entry.
- `LibertyRecomp/CMakeLists.txt` now conditionally adds the target-level specs option only when the cache-level link flags do not already contain `-specs=`.
- This keeps both the existing default build directory and the new guest-memory audit directory linkable without duplicate linker-script expansion.

Guest-memory audit behavior:

- With full eager audit sizes, the full app reached `Memory::Memory()` under ExeFS/NPDM and reported:
  - Alias region base `0x80000000`, size `0x180000000`
  - `SystemResourceSizeTotal=0x08000000`
  - `SystemResourceSizeUsed=0x1000`
  - first low guest heap mapping `0x4000000` failed with kernel `0x0000D001`
- `0x0000D001` decodes as Module Kernel, `KernelError_OutOfMemory`. Ryujinx also logged `MapPhysicalMemory() = OutOfMemory`.
- The guest-memory audit-stop build now skips function mapping insertion after successful audit mapping so the test can isolate SVC mapping capacity from generated function-table initialization.

Successful 96 MiB audit-window test:

- Configure sizes:
  - low guest memory: `0x2000000`
  - XMA I/O: `0x10000`
  - physical guest heap: `0x2000000`
  - image/function-table audit space: `0x2000000`
- Artifact:
  `C:\Users\Jellybone\Documents\Codex\2026-06-12\d-gta4-ns\work\liberty-switch-guest-memory-audit-debug\LibertyRecomp\LibertyRecompExefs.nsp`
- Build ID:
  `04e2f6197b65eb00c204af28d1d6e791718e779d`
- Ryujinx log:
  `D:\Games\Ryujinx\Ryujinx\portable\Logs\Ryujinx_Canary_1.3.269_2026-06-14_23-17-25.log`
- SD log:
  `D:\Games\Ryujinx\Ryujinx\portable\sdcard\switch\LibertyRecomp\LibertyRecomp.log`

Important Ryujinx lines:

```text
[Switch][Memory] map ok low guest heap
[Switch][Memory] map ok XMA I/O window
[Switch][Memory] map ok physical guest heap
[Switch][Memory] map ok image/function table
[Switch][Memory] allocate success
[Switch][Memory] audit stop build; skipping function mappings
[Switch] main entered.
[Switch] Switch guest memory audit mapped successfully; stopping before content preflight and host startup.
```

Earlier default build revalidation after the CMake/link changes:

- Earlier default ELF Build ID:
  `ffe15e3dba14a3bd2811fb42c6dbb5a4507b7439`
- Default ExeFS artifact:
  `C:\Users\Jellybone\Documents\Codex\2026-06-12\d-gta4-ns\work\liberty-switch-audit-debug\LibertyRecomp\LibertyRecompExefs.nsp`
- Default NRO artifact:
  `C:\Users\Jellybone\Documents\Codex\2026-06-12\d-gta4-ns\work\liberty-switch-audit-debug\LibertyRecomp\LibertyRecomp.nro`
- `readelf -dW` reports no `TEXTREL` for both default and guest-memory audit ELFs.
- Ryujinx default ExeFS smoke log:
  `D:\Games\Ryujinx\Ryujinx\portable\Logs\Ryujinx_Canary_1.3.269_2026-06-14_23-22-27.log`
- Default ExeFS still reports `guest memory disabled for startup audit; base=null`, reaches `main()`, skips RomFS in ExeFS/NSO mode, and stops at missing `sdmc:/switch/LibertyRecomp/game/default.xex`.

Current guest-memory conclusion:

- ExeFS/NPDM is the viable container path for sparse physical mapping; NRO/hbloader remains blocked by `SystemResourceSizeTotal=0`.
- Alias-region mapping works in the full process when the mapped windows are sized below the current budget.
- Eagerly mapping all currently planned windows is still too large. The next guest-memory step is not to enter guest code, but to design a demand/page-backed mapping strategy or otherwise reduce committed windows before function-table initialization and XEX loading.

## 2026-06-15 Update: Function-Table Mapping Audit

Created a new source branch for continued work:

`codex/switch-audit-20260615`

The prior GitHub backup branch remains:

`codex/github-backup-20260615-022231`

Source/audit note housekeeping:

- Mirrored the active audit notes into the main source repository under:
  `C:\Users\Jellybone\Documents\GitHub\LibertyRecomp\docs\switch-audit`
- The original active audit workspace remains:
  `C:\Users\Jellybone\Documents\Codex\2026-06-12\d-gta4-ns`

Build-system/runtime changes:

- Added `LIBERTY_RECOMP_SWITCH_GUEST_MEMORY_AUDIT_SKIP_FUNCTION_MAPPINGS`.
- `LIBERTY_RECOMP_SWITCH_GUEST_MEMORY_AUDIT_STOP` still controls the `main()` audit stop.
- The new skip flag controls whether `Memory::Memory()` returns before generated function-table initialization.
- Full app NPDM `system_resource_size` in `LibertyRecomp/os/switch/liberty_recomp_npdm.json` is now `0x10000000`.
- When function mappings are not skipped and no image/table size override is provided, `kernel/memory.cpp` scans `PPCFuncMappings` for the actual maximum guest address and maps enough of the image/function-table range to cover it.

Failed full-window function-mapping attempt:

- Configure:
  - `LIBERTY_RECOMP_SWITCH_GUEST_LOW_MEMORY_AUDIT_SIZE=0x2000000`
  - `LIBERTY_RECOMP_SWITCH_GUEST_PHYSICAL_HEAP_AUDIT_SIZE=0x2000000`
  - `LIBERTY_RECOMP_SWITCH_GUEST_IMAGE_TABLE_AUDIT_SIZE=`
  - `LIBERTY_RECOMP_SWITCH_GUEST_MEMORY_AUDIT_SKIP_FUNCTION_MAPPINGS=OFF`
  - `LIBERTY_RECOMP_SWITCH_GUEST_MEMORY_AUDIT_STOP=ON`
- Build ID before the NPDM budget increase:
  `0c7202746799915fbd9c680e3b5b9d46be697ebc`
- Ryujinx log:
  `D:\Games\Ryujinx\Ryujinx\portable\Logs\Ryujinx_Canary_1.3.269_2026-06-15_03-02-37.log`
- Result:
  - `low guest heap` mapped.
  - `XMA I/O window` mapped.
  - `physical guest heap` failed with `MapPhysicalMemory() = OutOfMemory`, `rc=0x0000D001`.
- Repeating after increasing NPDM `system_resource_size` to `0x10000000` still failed at the same `physical guest heap` step.
- Ryujinx log:
  `D:\Games\Ryujinx\Ryujinx\portable\Logs\Ryujinx_Canary_1.3.269_2026-06-15_03-05-11.log`
- Important line:
  `SystemResourceSizeTotal=0x10000000`

Failed pre-fix narrow function-mapping attempt:

- Configure:
  - `LIBERTY_RECOMP_SWITCH_GUEST_LOW_MEMORY_AUDIT_SIZE=0x1000`
  - `LIBERTY_RECOMP_SWITCH_GUEST_PHYSICAL_HEAP_AUDIT_SIZE=0x1000`
  - full image/function-table range from `PPC_CODE_SIZE`
  - function mappings not skipped
- Build ID:
  `8bf6a1da464c1241e505b52cff1166c3717cb5ce`
- Ryujinx log:
  `D:\Games\Ryujinx\Ryujinx\portable\Logs\Ryujinx_Canary_1.3.269_2026-06-15_03-13-21.log`
- Result:
  - low, XMA, physical, and image/function-table maps succeeded.
  - `Memory::Memory()` reached `inserting function mappings`.
  - Ryujinx reported invalid access at virtual address `0x00000001043D9000`.
- Root cause:
  - `PPCFuncMappings` contains entries beyond `PPC_CODE_BASE + PPC_CODE_SIZE`.
  - Example failing guest address in registers: `0x82A14918`.
  - The later scan found the actual max guest address `0x82A77E28`.
  - The old mapped size `0x23D8000` was too small for all table writes.

Successful narrow function-mapping audit:

- Build ID:
  `df2f6f7d433534156c4b0f6a19017e55d5302406`
- Artifact:
  `C:\Users\Jellybone\Documents\Codex\2026-06-12\d-gta4-ns\work\liberty-switch-guest-memory-audit-debug\LibertyRecomp\LibertyRecompExefs.nsp`
- Configure:
  - `LIBERTY_RECOMP_SWITCH_ENABLE_GUEST_MEMORY_AUDIT=ON`
  - `LIBERTY_RECOMP_SWITCH_GUEST_MEMORY_AUDIT_STOP=ON`
  - `LIBERTY_RECOMP_SWITCH_GUEST_MEMORY_AUDIT_SKIP_FUNCTION_MAPPINGS=OFF`
  - `LIBERTY_RECOMP_SWITCH_GUEST_LOW_MEMORY_AUDIT_SIZE=0x1000`
  - `LIBERTY_RECOMP_SWITCH_GUEST_PHYSICAL_HEAP_AUDIT_SIZE=0x1000`
  - `LIBERTY_RECOMP_SWITCH_GUEST_IMAGE_TABLE_AUDIT_SIZE=`
- Ryujinx log:
  `D:\Games\Ryujinx\Ryujinx\portable\Logs\Ryujinx_Canary_1.3.269_2026-06-15_03-18-05.log`
- SD log:
  `D:\Games\Ryujinx\Ryujinx\portable\sdcard\switch\LibertyRecomp\LibertyRecomp.log`
- Important Ryujinx lines:

```text
[Switch][Memory] function table max guest=0x82A77E28 mapped_size=0x24A0000
[Switch][Memory] map ok low guest heap
[Switch][Memory] map ok XMA I/O window
[Switch][Memory] map ok physical guest heap
[Switch][Memory] map ok image/function table
[Switch][Memory] allocate success
[Switch][Memory] inserting function mappings
[Switch][Memory] constructor complete
[Switch] main entered.
[Switch] Switch guest memory audit mapped successfully; stopping before content preflight and host startup.
```

Important SD log lines:

```text
[Switch] Switch audit package startup; continuing to content preflight.
[Switch] Switch guest memory audit mapped successfully; stopping before content preflight and host startup.
```

Current conclusion:

- Function-table initialization is no longer the immediate blocker when the image/function-table mapping covers the actual `PPCFuncMappings` maximum guest address.
- The next guest-memory blocker is committed mapping budget for eager low/physical windows once function mappings are retained. The `32 MiB` low + `32 MiB` physical heap + full image/function table path still fails before image mapping at the physical heap map.
- Do not enter guest code yet. The next design step is demand/page-backed guest memory, or another strategy that avoids eagerly committing all low/physical/image windows before content loading and runtime startup.

Default build revalidation after the 2026-06-15 function-table fix:

- Default ELF Build ID:
  `cb986fd2dc5e5554fdf9adfba8353aa2c2836d7d`
- Default NRO artifact:
  `C:\Users\Jellybone\Documents\Codex\2026-06-12\d-gta4-ns\work\liberty-switch-audit-debug\LibertyRecomp\LibertyRecomp.nro`
  - Size: `160,917,442` bytes
  - Last write time: `2026-06-15 03:21:56`
- Default ExeFS artifact:
  `C:\Users\Jellybone\Documents\Codex\2026-06-12\d-gta4-ns\work\liberty-switch-audit-debug\LibertyRecomp\LibertyRecompExefs.nsp`
  - Size: `64,968,931` bytes
  - Last write time: `2026-06-15 03:21:57`
- `readelf -dW` reports no `TEXTREL`.
- Ryujinx default ExeFS smoke log:
  `D:\Games\Ryujinx\Ryujinx\portable\Logs\Ryujinx_Canary_1.3.269_2026-06-15_03-23-35.log`
- Default ExeFS still reports `guest memory disabled for startup audit; base=null`, reaches `main()`, skips RomFS in ExeFS/NSO mode, appends the SD log, and stops at missing `sdmc:/switch/LibertyRecomp/game/default.xex`.

## 2026-06-15 Update: Guest-Memory Mapping Budget Audit

Added per-map SystemResource logging around the Switch sparse guest-memory `svcMapPhysicalMemory()` calls:

- Logs `SystemResourceSizeTotal` and `SystemResourceSizeUsed` before each map, after successful maps, and after failed maps.
- This is audit instrumentation only; it does not make the Switch artifact playable.

Build-system refinement:

- Moved guest-memory audit size and function-mapping-skip compile definitions from full-target compile definitions to `kernel/memory.cpp` source-file compile definitions.
- This keeps `LIBERTY_RECOMP_SWITCH_GUEST_MEMORY_AUDIT_STOP` target-wide for `main.cpp`, but changing low/physical/image audit sizes now rebuilds only `memory.cpp`, links, converts NSO, and packages ExeFS.
- Dry-run after this change showed `4` Ninja steps instead of rebuilding the full `133`-step target.
- CMake audit defaults are now:
  - `LIBERTY_RECOMP_SWITCH_GUEST_LOW_MEMORY_AUDIT_SIZE=0x1000`
  - `LIBERTY_RECOMP_SWITCH_GUEST_PHYSICAL_HEAP_AUDIT_SIZE=0xF00000`

Capacity audit results with function mappings retained and full scanned image/function-table mapping (`0x24A0000` bytes):

| Low window | Physical window | Build ID | Ryujinx log | Result |
| --- | --- | --- | --- | --- |
| `0x2000000` | `0x2000000` | `eb0f01eea431fc4302cf627d12ef04de727f6581` | `D:\Games\Ryujinx\Ryujinx\portable\Logs\Ryujinx_Canary_1.3.269_2026-06-15_10-26-05.log` | low and XMA mapped; physical heap failed with `0x0000D001` |
| `0x1000` | `0x2000000` | `2c7be87885669f71798ac0c6d269c66ef75aafb4` | `D:\Games\Ryujinx\Ryujinx\portable\Logs\Ryujinx_Canary_1.3.269_2026-06-15_10-35-32.log` | physical heap mapped; image/function table failed with `0x0000D001` |
| `0x1000` | `0x1000000` | `2b8b5735a15a7ff3a4f07530e73c4489a7d3bdc7` | `D:\Games\Ryujinx\Ryujinx\portable\Logs\Ryujinx_Canary_1.3.269_2026-06-15_10-45-54.log` | physical heap mapped; image/function table failed with `0x0000D001` |
| `0x1000` | `0x800000` | `78b8792864ad70e3b8428dd977a1cea1a04c3b9f` | `D:\Games\Ryujinx\Ryujinx\portable\Logs\Ryujinx_Canary_1.3.269_2026-06-15_10-48-31.log` | success; constructor complete; reached `main()` audit stop |
| `0x1000` | `0xC00000` | `723a05dcec43062b0b26b2c13f485b0e65a63f32` | `D:\Games\Ryujinx\Ryujinx\portable\Logs\Ryujinx_Canary_1.3.269_2026-06-15_10-51-06.log` | success; constructor complete; reached `main()` audit stop |
| `0x1000` | `0xE00000` | `d877e4e008452688330a9320cbd77e77b88d941b` | `D:\Games\Ryujinx\Ryujinx\portable\Logs\Ryujinx_Canary_1.3.269_2026-06-15_10-53-46.log` | success; constructor complete; reached `main()` audit stop |
| `0x1000` | `0xF00000` | `14293a41cb382b5346b04a31005477554db1cb56` | `D:\Games\Ryujinx\Ryujinx\portable\Logs\Ryujinx_Canary_1.3.269_2026-06-15_10-56-25.log` | success; constructor complete; reached `main()` audit stop |

Important observed lines from the latest successful `0xF00000` physical-window run:

```text
[Switch][Memory] function table max guest=0x82A77E28 mapped_size=0x24A0000
[Switch][Memory] map begin physical guest heap guest=0x80000000 size=0xF00000
[Switch][Memory] map ok physical guest heap
[Switch][Memory] map begin image/function table guest=0x82000000 size=0x24A0000
[Switch][Memory] map ok image/function table
[Switch][Memory] allocate success
[Switch][Memory] inserting function mappings
[Switch][Memory] constructor complete
[Switch] main entered.
[Switch] Switch guest memory audit mapped successfully; stopping before content preflight and host startup.
```

Current conclusion:

- `SystemResourceSizeUsed` stayed `0x1000` before and after both successful and failed maps in Ryujinx, so this specific Ryujinx counter is not useful for explaining the practical `0x0000D001` boundary.
- The retained-function-table audit can now map a stable startup set of `4 KiB` low memory, `64 KiB` XMA I/O, `15 MiB` physical heap, and the full scanned image/function-table range.
- `16 MiB` physical heap fails when the full scanned image/function table is also retained.
- The next runtime design step is still demand/page-backed guest memory: full eager low/physical mapping remains too expensive, but the Alias-region contiguous base model remains viable for generated code if pages can be committed selectively.

## 2026-06-15 Update: Startup-Memory / Xenon-Init Audit

Added a narrower Switch audit mode:

- `LIBERTY_RECOMP_SWITCH_GUEST_MEMORY_AUDIT_STOP_AFTER_XENON_INIT`

This mode is ExeFS/NPDM audit-only. It does not enter guest code and does not make the Switch artifact playable.

Runtime behavior:

- `Memory::Memory()` maps only the startup ranges needed for this phase:
  - low guest memory: `0x30000`
  - XMA I/O window: `0x10000`
  - physical heap window: `0xF00000`
  - Xenon fixed memory span: `0x82000000..0x831F0000` (`0x11F0000` bytes)
- Generated function-table insertion is skipped in this mode.
- `main()` bypasses host config, content preflight, installer, module loading, video creation, and guest thread startup.
- `main()` calls `KiSystemStartup()` only to run `g_userHeap.Init()` and `InitializeXenonMemoryRegions()`, then stops on a visible diagnostic.

Failed mapping-budget attempts before narrowing the image/table range:

| Low window | Physical window | Build ID | Ryujinx log | Result |
| --- | --- | --- | --- | --- |
| `0x30000` | `0xF00000` | `037ff1d12bd92cd4dfcdb529f55a1919c09c031f` | `D:\Games\Ryujinx\Ryujinx\portable\Logs\Ryujinx_Canary_1.3.269_2026-06-15_11-35-16.log` | image/function-table map failed with `0x0000D001` |
| `0x30000` | `0xE00000` | `c5ce87c3a9e9040f12b3efaf3ed276e3f89d4c1f` | `D:\Games\Ryujinx\Ryujinx\portable\Logs\Ryujinx_Canary_1.3.269_2026-06-15_11-38-00.log` | image/function-table map failed with `0x0000D001` |
| `0x30000` | `0xC00000` | `10ef8e9fb17761d0f7af54df92b7e4e5532b11a6` | `D:\Games\Ryujinx\Ryujinx\portable\Logs\Ryujinx_Canary_1.3.269_2026-06-15_11-43-25.log` | image/function-table map failed with `0x0000D001` |
| `0x30000` | `0x800000` | `35a8a278ea3e179dcf6771826dc40460b3883df7` | `D:\Games\Ryujinx\Ryujinx\portable\Logs\Ryujinx_Canary_1.3.269_2026-06-15_11-46-18.log` | image/function-table map failed with `0x0000D001` |

Root cause for those failures:

- The startup-memory audit initially still mapped the full scanned image/function-table range (`0x24A0000`) and retained function-table requirements that are not needed just to test `g_userHeap.Init()` plus Xenon fixed memory zeroing.
- Adding only `0x30000` low memory was enough to push the full function-table mapping over the practical `0x0000D001` boundary in this Ryujinx/NPDM setup.

Intermediate host-config finding:

- Build ID `bf80ba271b286653a1cafae185371755ea453619` mapped the narrowed Xenon fixed span successfully but continued into normal `Config::Load()`.
- Ryujinx log:
  `D:\Games\Ryujinx\Ryujinx\portable\Logs\Ryujinx_Canary_1.3.269_2026-06-15_11-57-47.log`
- Result:
  - `Memory::Memory()` completed startup-Xenon mapping.
  - `main()` reached `Startup-memory audit: before Config::Load.`
  - Ryujinx threw `Unknown MRS 0xD53B2521`, the known GCC 15 `gcspr_el0` unwinder/toolchain blocker, before `Config::Load()` returned.
- This is a host config / simulator-toolchain audit blocker, not evidence of guest gameplay behavior.

Successful startup-memory/Xenon-init audit:

- Build ID:
  `539c9829646e4f6e847b882ee8468d40cc579702`
- Artifact:
  `C:\Users\Jellybone\Documents\Codex\2026-06-12\d-gta4-ns\work\liberty-switch-guest-memory-audit-debug\LibertyRecomp\LibertyRecompExefs.nsp`
- Configure:
  - `LIBERTY_RECOMP_SWITCH_ENABLE_GUEST_MEMORY_AUDIT=ON`
  - `LIBERTY_RECOMP_SWITCH_GUEST_MEMORY_AUDIT_STOP=OFF`
  - `LIBERTY_RECOMP_SWITCH_GUEST_MEMORY_AUDIT_STOP_AFTER_XENON_INIT=ON`
  - `LIBERTY_RECOMP_SWITCH_GUEST_MEMORY_AUDIT_SKIP_FUNCTION_MAPPINGS=OFF`
  - `LIBERTY_RECOMP_SWITCH_GUEST_LOW_MEMORY_AUDIT_SIZE=0x30000`
  - `LIBERTY_RECOMP_SWITCH_GUEST_PHYSICAL_HEAP_AUDIT_SIZE=0xF00000`
  - `LIBERTY_RECOMP_SWITCH_GUEST_IMAGE_TABLE_AUDIT_SIZE=`
- Ryujinx log:
  `D:\Games\Ryujinx\Ryujinx\portable\Logs\Ryujinx_Canary_1.3.269_2026-06-15_12-03-45.log`
- SD log:
  `D:\Games\Ryujinx\Ryujinx\portable\sdcard\switch\LibertyRecomp\LibertyRecomp.log`
- `readelf -dW` still reports no `TEXTREL`.

Default package rebuild after these source changes:

- Default ExeFS Build ID:
  `e64961a3d8a222f6302772836b21d3cc2521996f`
- Artifact:
  `C:\Users\Jellybone\Documents\Codex\2026-06-12\d-gta4-ns\work\liberty-switch-audit-debug\LibertyRecomp\LibertyRecompExefs.nsp`
- Result:
  - `LibertyRecompExeFs` built successfully.
  - `readelf -dW` still reports no `TEXTREL`.
  - This was a build/metadata revalidation only, not a gameplay test.

Important Ryujinx lines:

```text
[Switch][Memory] Xenon fixed memory mapped_size=0x11F0000
[Switch][Memory] map ok low guest heap
[Switch][Memory] map ok XMA I/O window
[Switch][Memory] map ok physical guest heap
[Switch][Memory] map ok Xenon fixed memory
[Switch][Memory] allocate success
[Switch][Memory] startup-Xenon audit; skipping function mappings
[Switch] Switch startup-memory audit mapped guest memory; running KiSystemStartup heap/Xenon initialization only.
[InitializeXenonMemoryRegions] [Xenon] Zeroing stream pool: 0x82000000-0x82020000 (128 KB)
[InitializeXenonMemoryRegions] [Xenon] Zeroing XEX data region: 0x82020000-0x82120000 (1 MB)
[InitializeXenonMemoryRegions] [Xenon] Zeroing kernel runtime: 0x82A90000-0x82AA0000 (64 KB)
[InitializeXenonMemoryRegions] [Xenon] Zeroing static data (BSS): 0x83000000-0x831F0000 (1.99 MB)
[Switch] KiSystemStartup heap/Xenon memory initialization completed; stopping before host config, content, module load, and guest code.
```

Current conclusion:

- ExeFS/NPDM sparse Alias-region mapping can now support a minimal early-runtime container through `KiSystemStartup()` heap/Xenon fixed-memory initialization.
- This still does not load `default.xex`, does not initialize content/audio/video, does not insert generated function mappings, and does not run guest code.
- The next practical Switch blocker is deciding how to advance host config/content startup without relying on C++ exception/unwind paths that trigger the Ryujinx `gcspr_el0` limitation, then defining the real `sdmc:/switch/LibertyRecomp` game-content layout for module loading.
